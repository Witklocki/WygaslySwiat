using System.Collections.Generic;
using UnityEngine;

public class OwnGameManager : MonoBehaviour
{
    /*public static OwnGameManager Instance;

    // Use a List to store picked-up weapons
    public List<GameObject> pickedUpWeapons = new List<GameObject>();

    private void Awake()
    {
        if (Instance == null)
        {
            Instance = this;
            DontDestroyOnLoad(gameObject);
        }
        else
        {
            Destroy(gameObject);
        }
    }*/

}
